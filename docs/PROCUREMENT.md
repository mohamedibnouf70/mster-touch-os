# Procurement

Routes: `/procurement`, project tab **Commercial → Procurement**.

Flow: **PR → RFQ → Quotations → Comparison → Award approval → PO**.

Numbering: `MT-{PROJECT}-PR-{####}` via `generate_commercial_number`.

Permissions: `procurement.*`, `purchase_request.*`, `rfq.*`, `quotation.*`, `purchase_order.*`.

See `PHASE3_COMMERCIAL.md`.
