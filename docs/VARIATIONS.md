# Variations (Change Orders)

Table: `variations` — approved amount accumulates; **original contract value preserved** on `project_contracts`.

Approve RPC: `approve_variation(variation_id, approved_amount)`.

Links: RFI, NCR, correspondence (optional FKs).

Threshold rules: `approval_threshold_rules` (placeholder SAR bands — admin must confirm).
